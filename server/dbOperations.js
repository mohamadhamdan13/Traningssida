const db = require('./db'); // Importera din anslutningsmodul

function createExercise(exercise) {
  // Här kan du utföra insättningar i databasen
}

function getExercises() {
  // Här kan du hämta data från databasen
}

// Exportera funktionerna för användning i din huvudfil
module.exports = { createExercise, getExercises };
